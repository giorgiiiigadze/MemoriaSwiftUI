import SwiftUI

struct CreateDropView: View {
    var body: some View {
        PlaceholderScreen(title: "Create Drop", subtitle: "Create Drop flow — step 6")
    }
}

#Preview {
    CreateDropView()
}
